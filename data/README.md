# Data Directory

This directory will store local user data during development.

**Note:** All files in this directory are gitignored for privacy.

## Future data files:
- `users.json` - User preferences and settings
- `cache/` - Cached holiday data